query1
create table mydb.user (id int, name varchar(200));

user
SELECT * FROM mydb.user;
use mydb;
insert into user values (1, 'raymay');
insert into user values (2, 'マリオネッ。');
create table mydb.event (id int not null primary key auto_increment, name varchar(200));

event
SELECT * FROM mydb.event;

insert into event (name) values ( 'みんなのだいすきーこ');

SELECT * FROM mydb.event;

insert into event (name) values ( '｢片手分のファンタジー｣リリースアフターパーティー');

delete from event where id=2;

drop table user;
drop table idol;

create table mydb.idol (id int not null auto_increment primary key, name varchar(200));

SELECT * FROM mydb.idol;
insert into idol (name) values ( 'raymay');

insert into idol (name) values ( 'マリオネッ。');

delete from idol where id=3;

insert into idol (name) values ( 'NEMURIORCA');

drop table syutuen
create table mydb.syutuen (id int not null auto_increment primary key, idol_id int, event_id int);

insert into syutuen (idol_id, event_id) values ( '1', '1');

insert into syutuen (idol_id, event_id) values ( '2', '1');