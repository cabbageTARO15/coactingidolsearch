extends layout

block content
  h1 #{title}
  if noFoundName
    each idol in noFoundName
      p.errmsg 指定されたアイドル「#{idol}」が見つかりませんでした。
  form#mainform(method="get" action="/result")
    div
      label(for="idol1") 共演者を知りたいアイドルの名前を入力してください。
      input.form-control#idol1(type="text" name="idol1" required="true" placeholder="アイドル")
    div
      label(for="idol2", required=true) 共演者の名前を入力して下さい。
      input.form-control#idol2(type="text", name="idol2" required="true" placeholder="共演者")
    div
      input.btn.btn-primary(type="submit", value="そうしん！")
